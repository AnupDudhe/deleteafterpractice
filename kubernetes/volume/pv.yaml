apiVersion: v1
kind: PersistentVolume
metadata: 
  name: pvol
spec:
  capacity:
          storage: 10G
  volume:  filesystem
  accessModes:
            -ReadWriteOnce
  persistenceVolumeReclaimPolicy: Recycle
  storageClassName: standard
  hostPath:
      path: /mnt/data
