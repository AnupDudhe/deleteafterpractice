apiVersion: apps/v1 
kind:  StatefulSet 
metadata: 
    name: ssdb
    labels:
       app: ssdb
spec:
  replicas: 3 
  template:
      metadata: 
          name: ssdb
          labels:
              app: ssdb
      spec:
         containers:
            - name:  ssdb 
              image:  mysql:latest   
              ports:
                - containerPort: 3306
                  protocol: TCP 
              env:
                 - name: MYSQL_ROOT_PASSWORD
                   valueFrom:
                      secretKeyRef:
                        name: mysqldb-secret
                        key: root_pw
  selector:
    matchLabels: 
      app: ssdb    
              #docker run -d -P -e "MYSQL_ROOT_PASSWORD"="1234"