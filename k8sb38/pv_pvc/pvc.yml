apiVersion: v1 
kind: PeristentVolumeClaim
metadata:
     name: pv-claim 
     labels:
       app: pv-claim
spec:
    storageClassName: standard
    volumeMode: Filesystem
    accessModes:
      - ReadWriteOnce
    resources:
       requests:
         storage: 16GI