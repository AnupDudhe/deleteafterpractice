apiVersion: v1
kind: Service
metadata:  
  name: clusteripsrvmob
spec:
    type: ClusterIP
    selector:
           app: deployappmob
    ports:
        - port: 80
          targetPort: 80
          name: http
          protocol: TCP
          
